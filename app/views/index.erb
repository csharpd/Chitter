<ul>
  <% @peeps.reverse.each do |peep| %>
    <h2><%= peep.content %></h2>
    <h3>Added on <%= peep.created_at.strftime("%m/%d/%y at %I:%M%p") %> by <%= peep.user.name %> | @<%= peep.user.username %> </h3>
  <% end %>
</ul>


